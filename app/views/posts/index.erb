 <div class="jumbotron">
   <h1 class="display-3">게시판 입니당.</h1>
   <p class="lead">님들 이야기를 남기면 되영</p>
   <hr class="my-4">
   <p>하하하하하</p>

 </div>

  <table class="table table-dark">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">제목</th>
        <th scope="col">글쓴이</th>
        <th scope="col">작성일</th>
      </tr>
    </thead>
    <tbody>
 <% @posts.each do |post| %>
      <tr>
        <th scope="row"><%= post.id %></th>
        <td><a href="/posts/show/<%= post.id %>"><%= post.title %></td></a>
        <td><%= post.user.email %></td>
        <td><%= time_ago_in_words(post.created_at) %></td>
         <% end %>
      </tr>
    </tbody>
  </table>
  <div class="row">
  <div class="col-md-2 ml-auto">
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="/posts/new" role="button">[새 글 쓰깅]</a>
  </p>
  </div>
  </div>
  <br><br><br><br><br><br><br><br>
